{{ form_start(form) }}
{{ form_widget(form) }}
<button class="btn btn-primary">{{ button_label|default('Valider') }}</button>
<a class="btn btn-outline-success" href="{{ path('app_home') }}">Revenir</a>

{{ form_end(form) }}
